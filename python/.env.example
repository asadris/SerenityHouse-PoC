# Serenity House — Database Connection Configuration
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# ── Local SQL Server (development) ──────────────────────────
# SH_SERVER=PETRISLAP2025\PETRIS2022
# SH_DATABASE=Serenity1
# SH_AUTH=windows          # windows = Trusted_Connection; sql = username+password

# ── Azure SQL Database (production) ─────────────────────────
SH_SERVER=serenityhousepeter.database.windows.net
SH_DATABASE=SerenityHouse
SH_AUTH=sql
SH_USER=your_username
SH_PASSWORD=your_password
